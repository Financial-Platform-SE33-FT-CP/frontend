# Auth service (FastAPI) — paths like /auth/login. Defaults to 8001 for local uvicorn.
NEXT_PUBLIC_AUTH_URL=http://localhost:8001

# Other APIs (tenant, COA, …) when called directly; often unused if you only use AUTH for now.
NEXT_PUBLIC_API_URL=http://localhost:8000
